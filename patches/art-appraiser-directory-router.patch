diff --git a/src/router.tsx b/src/router.tsx
index [HASH]...[HASH] 100644
--- a/src/router.tsx
+++ b/src/router.tsx
@@ -36,5 +36,5 @@ export const router = createBrowserRouter([
     },
   },
 ], {
-  basename: '/' // Ensure the router uses the correct base path
+  basename: '/directory' // Use /directory as the base path when used in the monorepo
 }); 
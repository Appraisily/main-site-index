/**
 * ESLint configuration for submodule
 * Extends the shared ESLint base configuration
 */

module.exports = {
  extends: ['../shared/config/eslint.base.js'],
  // Add submodule-specific rules below
  rules: {
    // Override or add rules specific to this submodule
  },
  settings: {
    // Submodule-specific settings
  },
}; 